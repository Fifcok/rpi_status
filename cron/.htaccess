# Skrypty przeznaczone wyłącznie do uruchamiania z crontaba (CLI) - blokada dostępu przez HTTP.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
