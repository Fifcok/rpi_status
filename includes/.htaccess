# Katalog includes/ nie powinien być nigdy dostępny bezpośrednio z przeglądarki.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
